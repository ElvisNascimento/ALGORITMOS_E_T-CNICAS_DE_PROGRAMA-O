// 1) Faça um algoritmo que leia os valores A, B, C e imprima na tela se a soma de A + B é menor
// que C.
let A =2;
let B =3;
let C =12;

if(A + B < C)
{
    document.write("a soma dos valores de A + B é menor que o valor de C");
}